"""Legacy webhook stub.

The active payment integration uses Tokoshopp's documented polling endpoint
`POST /api/payment/status`; the bot does not start this module.
Kept as a harmless stub so an old external import does not break.
"""
